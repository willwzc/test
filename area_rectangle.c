#include <stdio.h>
int main(void)
{
    int side1, side2, area;
    side1 = 7;
    side2 = 8;
    area = side1 * side2;

    printf("side1 is %i\n", side1);
    printf("side2 is %i\n", side2);
    printf("area is %i\n", area);

    return 0;
}
